{
  "name": "crypto-agility-stress-test",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive enterprise crypto-agility tabletop simulation.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && npm run build"
  },
  "engines": {
    "node": ">=18"
  }
}
